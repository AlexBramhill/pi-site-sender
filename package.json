{
  "scripts": {
    "dev": "docker compose up --build -f docker-compose.dev.yml",
    "prod": "docker compose up --build -f docker-compose.prod.yml"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}

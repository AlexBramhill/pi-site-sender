services:
  screenshot-processor:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: screenshot-processor:latest
    environment:
      - NODE_ENV=production
    restart: unless-stopped

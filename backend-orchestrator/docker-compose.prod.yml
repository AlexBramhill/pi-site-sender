services:
  backend-orchestrator:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: backend-orchestrator:latest
    environment:
      - NODE_ENV=production
    ports:
      - "4001:4001"
    restart: unless-stopped
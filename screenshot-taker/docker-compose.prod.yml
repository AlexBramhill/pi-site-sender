services:
  screenshot-taker:
    build:
      context: .
      dockerfile: Dockerfile.prod
    image: screenshot-taker:latest
    environment:
      - NODE_ENV=production
    ports:
      - "4001:4001"
    restart: unless-stopped
services:
  screenshot-taker:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: screenshot-taker:dev
    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=development
    ports:
      - "4000:4000"
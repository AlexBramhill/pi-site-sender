services:
  site:
    extends:
      file: ./site/docker-compose.dev.yml
      service: site

  redis:
    extends:
      file: ./site/docker-compose.prod.yml
      service: redis

  photo:
    extends:
      file: ./photo/docker-compose.dev.yml
      service: photo

  backend-orchestrator:
    extends:
      file: ./backend-orchestrator/docker-compose.dev.yml
      service: backend-orchestrator

networks:
  shared-network:
    driver: bridge

volumes:
  redis_data:
services:
  site:
    extends:
      file: ./site/docker-compose.dev.yml
      service: site

  photo:
    extends:
      file: ./photo/docker-compose.dev.yml
      service: photo

networks:
  shared-network:
    driver: bridge

services:
  screenshot-processor:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: screenshot-processor:dev
    volumes:
      - ./src:/app/src
    environment:
      - NODE_ENV=development
    ports:
      - "4002:4002"
